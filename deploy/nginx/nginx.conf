user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /var/run/nginx.pid;

events {
    worker_connections  1024;
    multi_accept        on;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';
    access_log  /var/log/nginx/access.log  main;

    resolver 127.0.0.11 valid=10s ipv6=off;

    map $http_origin $cors_origin {
        default "";
        ~^https?://(localhost|127\.0\.0\.1)(:[0-9]+)?$ $http_origin;
        ~^https://([A-Za-z0-9-]+\.)*omnip\.org$ $http_origin;
        ~^https://([A-Za-z0-9-]+\.)*altapano\.com$ $http_origin;
    }

    sendfile        on;
    keepalive_timeout  65;

    client_max_body_size 20m;

    # Per-client request ceiling for the whole API surface. cloudflare-realip.conf
    # restores the real client address into $binary_remote_addr, so this keys on
    # the visitor rather than the Cloudflare edge. Tune rate/burst from access
    # logs; /webhooks/ is exempt in default.conf because Stripe retries from a
    # small fixed IP set.
    limit_req_zone  $binary_remote_addr zone=api_general:10m rate=10r/s;
    limit_conn_zone $binary_remote_addr zone=api_conn:10m;
    limit_req_status  429;
    limit_conn_status 429;

    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 5;
    gzip_min_length 256;
    gzip_types
        application/json
        application/javascript
        application/xml
        text/css
        text/javascript
        text/plain
        text/xml
        image/svg+xml;

    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;
    ssl_session_tickets off;

    include /etc/nginx/conf.d/cloudflare-geo.conf;
    include /etc/nginx/conf.d/cloudflare-realip.conf;

    include /etc/nginx/conf.d/default.conf;
}
