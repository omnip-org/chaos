{
  "name": "chaos-provision-store",
  "private": true,
  "type": "module",
  "description": "Provisions a declarative catalog into one Chaos Store over MCP. Run once per Store (e.g. once for a test Store, once for a live Store) so both share the same catalog definition.",
  "scripts": {
    "provision": "node provision.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  }
}
